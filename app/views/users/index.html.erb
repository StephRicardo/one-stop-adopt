This is our user's index view!! User landing page!!

<ul>
	<% @users.each do |user| %>
		<li>
			<%= "#{user.first_name} #{user.last_name}" %>
		</li>
	<% end %>
</ul>

<%= link_to "Log out", logout_path, method: :delete %>