<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.first_name %> <%= @user.last_name %>
      </h1>
    </section>
  </aside>
</div>

<div>
	<% current_user.selected_pets.each do | spet | %>
		<%= spet.pet.name %>
	<% end %>

</div>
